This directory stores the reults of evaluation.

For example, `llama3/vanilla/qgeval-answerability.xlsx` is the evaluation result of LLaMA-3 using vanilla prompt on the `answerability` dimension.

All results will be released later.